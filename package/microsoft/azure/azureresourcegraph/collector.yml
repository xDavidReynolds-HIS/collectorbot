classes:
    # Name of schema artifact that contains the classes below
    - "@zerobias-org/schema-microsoft-azure":
        # Azure Resource Graph `Resources` table rows (listResources)
        - AzureInventoryItem
        # Azure Resource Graph `ResourceContainers` table rows
        # (listResourceContainers), one suite class per row kind
        - AzureSubscription
        - AzureResourceGroup
        - AzureManagementGroup
